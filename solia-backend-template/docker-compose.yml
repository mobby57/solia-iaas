version: "3.8"

services:
  backend:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - PORT=3000
      - DATABASE_URL=${database_url}
      - JWT_SECRET=${JWT_SECRET}
    volumes:
      - .:/app
    command: npm run dev
